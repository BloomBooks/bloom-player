.*
node_modules
# as far as I can tell, source maps are only useful on the system where they were built,
# where the source files are actually available at the indicated address.
output/*.map
# We might want some of these eventually, but at this point, I don't even know
# where they are coming from, so no point in pushing them into npm.
output/*.gif
output/*.eot
output/*.ttf
output/*.svg